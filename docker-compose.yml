version: '3.8'
services:
  lsp-server:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./workspaces:/workspaces
    environment:
      - JAVA_OPTS=-Xms1G -Xmx2G
    restart: unless-stopped
    stdin_open: true
    tty: true
    